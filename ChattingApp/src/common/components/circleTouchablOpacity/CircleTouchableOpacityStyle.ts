import { StyleSheet } from 'react-native'

const touchaleOpacityStyles = StyleSheet.create({
    circleTouchableOpacity: {
        width: 60,
        height: 60,
        borderRadius: 60 / 2,
        borderColor: 'pink',
        borderWidth: 1,
        backgroundColor: 'white',
        alignItems: 'center',
        justifyContent: 'center'
    }
})

export default touchaleOpacityStyles